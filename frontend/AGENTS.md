<!-- LOVABLE:BEGIN -->
> [!IMPORTANT]
> This project is connected to [Lovable](https://lovable.dev). Avoid rewriting
> published git history — force pushing, or rebasing/amending/squashing commits
> that are already pushed — as it rewrites history on Lovable's side and the
> user will likely lose their project history.
>
> Commits you push to the connected branch sync back to Lovable and show up in
> the editor, so keep the branch in a working state.
<!-- LOVABLE:END -->

- Keep Evidra's forensic workflow frontend-only with deterministic mock evidence and explicit demo labels; no credential, hardware, custody, hash-verification, or ingest claims may imply real processing until backed by a verified service.
- Keep the case locker separate from the five investigation routes; only Alpha opens the populated workspace because the other cards have no evidence dataset.
